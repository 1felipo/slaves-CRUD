<%- include("./partials/header") %>

<div class="container">
<div class="row">
<div class="col-md-5 offset-md-3">
    <div class="card">
        <div class="card-body">
            <form action="/edit/<%= slave._id %>" method="post">
                <div class="form-group">
                    <input type="text" 
                    name="slave" 
                    placeholder="Insert the slave's name..." 
                    class="form-control"
                    value="<%= slave.slave %>">
                </div>
                <div class="form-group">
                    <textarea name="description" 
                    cols=80 
                    class="form-control" 
                    placeholder="About the slave..."><%= slave.description %></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Edit slave 😋</button>
            </form>
        </div>
    </div>
</div>
</div>
</div>

<%- include("./partials/footer") %>